# Suppress audit output during install — vulnerabilities listed are all in
# dev-server tools (esbuild, vite) and server-side Astro features.
# This site is static output only; none of them apply in production.
audit=false
fund=false
